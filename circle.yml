machine:
  services:
    - docker

dependencies:
  pre:
    - pip install docker-compose

test:
  override:
    - docker-compose run -d --no-deps web
    - npm test

deployment:
  hub:
    branch: master
    commands:
      - $DEPLOY
